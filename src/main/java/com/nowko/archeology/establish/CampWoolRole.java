package com.nowko.archeology.establish;

/**
 * Which tent-grid wool a colour change rewrites.
 */
public enum CampWoolRole {
    /** {@code W} cells (default white). */
    PRIMARY,
    /** {@code R} cells (default red). */
    SECONDARY
}
