package com.nowko.archeology.model;

/**
 * Kind of site dossier.
 */
public enum SiteType {
    /** Staff-registered ruin (data only, no vanilla structure). */
    MANAGED_RUIN,
    /** Player excavation camp (future). */
    EXCAVATION
}
